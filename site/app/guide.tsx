'use client';
import { useState } from 'react';
import { useVideoSource } from '@/hooks/use-video-source';
import { ArrowUpRight, Copy, Check, Printer } from 'lucide-react';
import { Button } from '@/components/ui/button';
import { Sidebar, SidebarContent, SidebarFooter, SidebarHeader, SidebarInset, SidebarMenu, SidebarMenuButton, SidebarMenuItem, SidebarProvider, SidebarTrigger, useSidebar } from '@/components/ui/sidebar';
const source = 'https://github.com/dev7a/lnpctl';
const chapters = [['before','Before you begin'],['build','Build and open'],['select','Select and prepare'],['recovery','Enter Recovery'],['apply','Apply and reboot'],['restore','Restore a backup'],['help','Troubleshooting and development']];
function Contents() {
  const { setOpenMobile } = useSidebar();
  return <Sidebar className="manual-sidebar"><SidebarHeader className="px-7 pt-9 pb-4"><a href="/" className="brand">lnpctl<span>_</span></a><p className="text-sm text-slate-400 mt-1">Local Network Privacy Control</p><p className="rail-label">The field guide</p></SidebarHeader><SidebarContent className="px-4"><nav aria-label="Guide chapters"><SidebarMenu>{chapters.map(([id,label],i)=><SidebarMenuItem key={id}><SidebarMenuButton render={<a href={'#'+id}/>} onClick={()=>setOpenMobile(false)} className="h-auto min-h-11 py-3 px-3 text-[15px]"><span className="nav-number">{String(i+1).padStart(2,'0')}</span>{label}</SidebarMenuButton></SidebarMenuItem>)}</SidebarMenu></nav></SidebarContent><SidebarFooter className="px-7 py-7 border-t border-sidebar-border"><p className="text-[#ffac83] text-sm font-medium">Very experimental</p><p className="text-sm text-slate-400 mt-1">Use at your own risk and peril.</p><a className="flex items-center gap-2 mt-5 text-sm" href={source}>Source on GitHub <ArrowUpRight size={15}/></a></SidebarFooter></Sidebar>;
}
function Command({children,label='Terminal'}:{children:string;label?:string}) {
  const [state,setState]=useState('Copy');
  async function copy() { try { await navigator.clipboard.writeText(children);setState('Copied'); } catch { setState('Select text to copy'); } }
  return <div className="code-block"><div className="code-head"><span>{label}</span><Button variant="ghost" className="copy-button" onClick={copy} aria-label={'Copy '+label+' command'}>{state==='Copied'?<Check size={14}/>:<Copy size={14}/>}<span aria-live="polite">{state}</span></Button></div><pre tabIndex={0}><code>{children}</code></pre></div>;
}
function Figure({src,alt,caption}:{src:string;alt:string;caption:string}) { return <figure className="figure"><a className="image-link" href={src} aria-label={'Open full-size image: '+alt}><img src={src} alt={alt} loading="lazy"/></a><figcaption>{caption}</figcaption></figure>; }
function Section({id,number,title,children}:{id:string;number:string;title:string;children:React.ReactNode}) { return <section id={id} className="guide-section"><div className="section-title"><span className="section-number">{number}</span><h2>{title}</h2></div>{children}</section>; }
export default function Guide() {
  const pickerSource = useVideoSource('/media/lnpctl-demo.mp4');
  return <SidebarProvider style={{'--sidebar-width':'16rem'} as React.CSSProperties}><a href="#content" className="skip-link">Skip to the guide</a><Contents/><SidebarInset><header className="site-header"><div className="flex items-center gap-2"><SidebarTrigger className="md:hidden"/><span className="mobile-title">lnpctl</span><span className="desktop-header-title">Tutorial / macOS</span></div><div className="header-links"><span className="experimental">Experimental</span><span className="version">v0.1.3</span><Button variant="ghost" className="print-button" onClick={()=>window.print()} aria-label="Print the guide"><Printer size={16}/><span className="hidden sm:inline">Print</span></Button></div></header><main id="content" className="article"><div id="before"><p className="eyebrow">Local Network permissions / Apple silicon</p><h1>Clean up stale Local<br/>Network permissions.</h1><p className="intro">lnpctl lets you select stale Local Network permission entries, prepare a backup, and apply the cleanup from macOS Recovery.</p><aside className="warning" aria-label="Experimental software warning"><h2>Very experimental. Run it at your own risk and peril.</h2><p>This uses private CoreFoundation APIs and directly edits an undocumented macOS configuration format. A bug, a wrong selection, or a macOS update could break network access or damage settings. The backup and validation checks are not a guarantee.</p><p>Back up your Mac first. Try a disposable VM before a Mac you depend on. If you cannot afford to troubleshoot Recovery, do not run this.</p></aside><div className="facts"><span><strong>Hardware</strong> Apple silicon</span><span><strong>Build target</strong> macOS 15+</span><span><strong>Tested workflow</strong> macOS 26.6.2 + 27 beta 7 (VMs)</span></div><Section id="requirements" number="01" title="Before you begin"><p>You need Xcode or Command Line Tools to build, an administrator account, and time to shut down and enter Recovery. Keep System Integrity Protection (SIP) enabled.</p><p>Choose only entries you recognize. A missing executable can help you identify an old build, but it does not prove the entry is unwanted.</p><div className="stage-strip"><div><span>Normal macOS</span><strong>Select & prepare</strong>Save a backup. Permissions stay as they are.</div><div><span>macOS Recovery</span><strong>Apply the cleanup</strong>Write the reviewed change to the offline volume.</div><div><span>Normal macOS</span><strong>Verify after reboot</strong>Check settings and retained apps.</div></div></Section></div><Section id="build" number="02" title="Build and open"><p>Clone the source into a local folder, then build it. The repository is currently private; you need access to clone it.</p><Command label="Normal macOS">{'git clone https://github.com/dev7a/lnpctl.git\ncd lnpctl\nmake\nsudo ./build/lnpctl'}</Command><p>The two private APIs are <code>_CFKeyedArchiverUIDGetValue</code> and <code>_CFKeyedArchiverUIDGetTypeID</code>. They inspect references inside the plist archive. The tool itself removes selected rule references and rewrites <code>com.apple.networkextension.plist</code>; it does not use a supported Local Network permission-management API. Both the private APIs and the undocumented format may change across macOS versions.</p><p>The executable uses Apple’s system libraries. It needs no Python or Homebrew to run. Opening the picker reads the permission store; it does not change permissions or create a backup.</p></Section><Section id="select" number="03" title="Select and prepare">
<p>Close the apps whose entries you intend to remove, and close System Settings. In the picker, choose only entries you know are unwanted.</p>
<Figure src="/media/01-picker.png" alt="lnpctl permission picker with application identities and executable paths" caption="The picker uses demonstration data. Check the identity and path on your own Mac before selecting anything."/>
<ul><li>Use the arrow keys to move and <kbd>Space</kbd> to select or deselect a row.</li><li>Press <kbd>/</kbd> to filter. Selections remain selected when the filter hides them.</li><li>Press <kbd>Tab</kbd> for details. “Not recorded” means the archive has no executable path; it does not mean the executable is missing.</li><li>Press <kbd>Enter</kbd> to review every selected entry. Press <kbd>p</kbd> to prepare the backup, or <kbd>q</kbd> to cancel.</li></ul>
<Figure src="/media/05-review.png" alt="Review screen showing selected entries before preparation" caption="Review the complete selection, including entries hidden by a filter. Preparation saves files; it does not change permissions."/>
<video src={pickerSource} className="video" controls playsInline preload="metadata" poster="/media/01-picker.png" aria-label="Keyboard picker demonstration">Your browser cannot play this video. <a href="/media/lnpctl-demo.mp4">Download the picker demonstration.</a></video>
<p className="note">The short picker video shows a real ncurses interface with synthetic application data.</p>
<h3>Save the Recovery checklist before shutting down</h3>
<p>Preparation prints the Recovery command and writes a <code>RECOVERY.txt</code> checklist with the backup. Photograph or print those instructions now; you may not be able to open this website in Recovery.</p>
<p>The default backup location is <code>/Users/Shared/lnpctl/backups/</code>. Each backup includes the original store, the prepared change, a manifest, and the executable that prepared it. These files contain private application and configuration information. Keep them private and do not move or edit them.</p>
</Section>
<Section id="recovery" number="04" title="Enter macOS Recovery">
<ol><li>Shut down your Apple silicon Mac.</li><li>Hold the power button until startup options appear. Select <strong>Options → Continue</strong>.</li><li>Authenticate if prompted, then open <strong>Disk Utility</strong>.</li><li>Choose <strong>View → Show All Devices</strong>. Select the Data volume beneath your startup disk and click <strong>Mount</strong>. Unlock it if prompted. If it is already mounted, leave it mounted. Do not choose Erase or Restore.</li></ol>
<Figure src="/media/recovery/02-options-continue.jpg" alt="Startup options with Options selected and Continue available" caption="Choose Options, then Continue. These are unedited screenshots from a disposable Tart VM; your screens and volume names may differ."/>
<Figure src="/media/recovery/06-data-unmounted.jpg" alt="Disk Utility with the Data volume selected and Mount available" caption="Mount the correct Data volume. This guest was unmounted and remounted only to demonstrate the two states."/>
<p>Confirm the mount point in Disk Utility. Choose <strong>Disk Utility → Quit Disk Utility</strong>, then <strong>Utilities → Terminal</strong> from the Recovery menu bar.</p>
<Figure src="/media/recovery/08-utilities-terminal.jpg" alt="Recovery Utilities menu with Terminal selected" caption="Open Terminal from the menu bar after leaving Disk Utility."/>
<Command label="Recovery Terminal">{'ls /Volumes'}</Command>
<p>Use the launcher command saved during preparation. With the default backup location and a Data volume mounted at <code>/Volumes/Data</code>, it is:</p>
<Command label="Recovery Terminal">{"'/Volumes/Data/Users/Shared/lnpctl/backups/lnpctl-recovery'"}</Command>
<p>Keep the quotes. Replace <code>Data</code> with the actual mount name if yours differs. Use the exact capitalization from your saved command. Keep SIP enabled.</p>
</Section>
<Section id="apply" number="05" title="Apply, then reboot">
<p>Choose the prepared backup by its date and removal count. The screenshot’s backup number is only an example. Review the application identities and paths again.</p>
<Figure src="/media/recovery/13-review-apply.jpg" alt="Recovery launcher reviewing the selected cleanup and offering apply or restore" caption="This run removes one controlled stale entry named LNP Remove in the disposable VM."/>
<aside className="warning"><h3>This is the step that writes the change.</h3><p>Very experimental. Continue at your own risk and peril. Enter <code>a</code> to apply, then enter <code>y</code> at “Apply this prepared cleanup? [y/N]” only if the selection is correct. Return cancels the confirmation.</p></aside>
<p>Wait for <strong>Cleanup completed and verified.</strong> If the tool refuses the change or reports an error, read the message and follow the refusal guidance below.</p>
<Figure src="/media/recovery/15-cleanup-complete.jpg" alt="Recovery Terminal reporting Cleanup completed and verified" caption="The tool verifies the installed cleanup and asks you to reboot. It does not reboot automatically."/>
<Command label="After successful cleanup">{'reboot'}</Command>
<p>After login, open <strong>System Settings → Privacy &amp; Security → Local Network</strong>. Check that only the selected entries are gone, and test network access in the applications you kept.</p>
</Section>
<Section id="restore" number="06" title="Restore a backup">
<p>Return to Recovery, mount the same Data volume, and open the saved launcher command. Choose the backup you want to restore, enter <code>r</code>, and confirm with <code>y</code> only after checking the displayed backup. Wait for successful completion, then reboot and test your settings and connectivity.</p>
<aside className="warning"><h3>Restore replaces the whole main NetworkExtension plist.</h3><p>It also rolls back later changes stored in that file. It is not an undo for just the selected rows. The tool saves a restore-safety snapshot before replacing the file.</p></aside>
<p>Current version 0.1.3 restore tests use an offline APFS test volume. A full Recovery restore and normal reboot cycle was tested with an older 0.1.0 build, not with 0.1.3.</p>
</Section>
<Section id="help" number="07" title="Troubleshooting and development">
<h3>The launcher path does not exist</h3><p>Check <code>ls /Volumes</code>, mount or unlock the correct Data volume, and compare the command with the checklist you saved. A custom backup location needs its own saved path.</p>
<h3>The source changed after preparation</h3><p>Return to normal macOS and prepare a fresh backup. Do not force the old plan onto the changed store.</p>
<h3>The tool refuses the volume, backup, or archive</h3><p>Stop and read the exact message. The tool rejects the currently booted Data volume, wrong-volume plans, tampered backups, and unsupported or ambiguous archive layouts. Do not disable SIP or change file permissions to bypass a refusal.</p>
<h3>How this was built</h3>
<p>lnpctl is written in Objective-C and uses Apple’s system libraries, with ncurses for the keyboard-driven interface. It builds with Xcode or Command Line Tools and needs no Python or Homebrew at runtime.</p>
<p>Development combined automated archive, terminal, and CLI tests with offline APFS checks and full cleanup runs in disposable Tart VMs on macOS 26.6.2 and macOS 27 beta 7. The cleanup has also been used successfully on the developer’s own Mac.</p>
<p>The walkthrough was recorded in a VM: selection in Ghostty, application in Recovery, and verification after reboot. SIP stayed enabled. The edited video adds narration, subtitles, and close-ups of the key steps.</p>
<p>These successful runs do not establish safety on every Mac or macOS version. FileVault unlock and a full Recovery restore cycle with 0.1.3 are not covered by the recorded tests.</p>
<p><a href={source+'/blob/main/docs/validation.md'}>Read the development test record</a> or <a href={source+'/blob/main/docs/recovery-walkthrough.md'}>see the Recovery screenshots</a>. Repository access is required.</p>
</Section><footer className="end-note">lnpctl v0.1.3 · Guide updated September 9, 2026.<br/>Experimental software. No supported Apple reset API is used. <a href={source}>Read the source before trusting it with your settings.</a></footer></main></SidebarInset></SidebarProvider>; }
